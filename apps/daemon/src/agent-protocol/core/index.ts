/** @module agent-protocol/core
 * Foundation barrel for the agent-protocol module. Re-exports the JSON-line
 * stream parser used by the Pi RPC adapter.
 */
export { createJsonLineStream } from './json-line-stream.js';
