#!/usr/bin/env bash
# Deterministic Claude Code wrapper for local tests.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
exec node "$HERE/../mock-agent.mjs" --as claude "$@"
